package com.koalog.jcs.examples;

import junit.framework.TestCase;
import org.apache.log4j.Category;
import com.koalog.jcs.solver.Solver;
import com.koalog.jcs.solver.DefaultSplitSolver;

/**
 * Tests Schur's Lemma problem.
 * 
 * @author Yan Georget
 */
public class SchurLemmaTest extends TestCase {
    //------------------------------------------------------------------------
    // CONSTRUCTORS
    //------------------------------------------------------------------------
    /**
     * Sole constructor.
     * @param name a test name
     */
    public SchurLemmaTest(String name) {
        super(name);
    }
    
    //------------------------------------------------------------------------
    // METHODS
    //------------------------------------------------------------------------
    /**
     * Tests Schur's lemma of size 1.
     */
    public void testSchurLemma1() {
        cat.info("testSchurLemma1");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(1));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 2.
     */
    public void testSchurLemma2() {
        cat.info("testSchurLemma2");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(2));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 3.
     */
    public void testSchurLemma3() {
        cat.info("testSchurLemma3");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(3));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 4.
     */
    public void testSchurLemma4() {
        cat.info("testSchurLemma4");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(4));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 5.
     */
    public void testSchurLemma5() {
        cat.info("testSchurLemma5");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(5));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 6.
     */
    public void testSchurLemma6() {
        cat.info("testSchurLemma6");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(6));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 7.
     */
    public void testSchurLemma7() {
        cat.info("testSchurLemma7");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(7));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 8.
     */
    public void testSchurLemma8() {
        cat.info("testSchurLemma8");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(8));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 9.
     */
    public void testSchurLemma9() {
        cat.info("testSchurLemma9");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(9));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 10.
     */
    public void testSchurLemma10() {
        cat.info("testSchurLemma10");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(10));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 11.
     */
    public void testSchurLemma11() {
        cat.info("testSchurLemma11");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(11));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 12.
     */
    public void testSchurLemma12() {
        cat.info("testSchurLemma12");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(12));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 13.
     */
    public void testSchurLemma13() {
        cat.info("testSchurLemma13");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(13));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 14.
     */
    public void testSchurLemma14() {
        cat.info("testSchurLemma14");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(14));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 15.
     */
    public void testSchurLemma15() {
        cat.info("testSchurLemma15");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(15));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 16.
     */
    public void testSchurLemma16() {
        cat.info("testSchurLemma16");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(16));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 17.
     */
    public void testSchurLemma17() {
        cat.info("testSchurLemma17");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(17));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 18.
     */
    public void testSchurLemma18() {
        cat.info("testSchurLemma18");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(18));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 19.
     */
    public void testSchurLemma19() {
        cat.info("testSchurLemma19");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(19));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 20.
     */
    public void testSchurLemma20() {
        cat.info("testSchurLemma20");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(20));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 21.
     */
    public void testSchurLemma21() {
        cat.info("testSchurLemma21");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(21));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 22.
     */
    public void testSchurLemma22() {
        cat.info("testSchurLemma22");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(22));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

    /**
     * Tests Schur's lemma of size 23.
     */
    public void testSchurLemma23() {
        cat.info("testSchurLemma23");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(23));
        s.solve(1);
        assertTrue(s.getSolutions().size() != 0);
    }

     /**
     * Tests Schur's lemma of size 24.
     */
    public void testSchurLemma24() {
        cat.info("testSchurLemma24");
        Solver s = new DefaultSplitSolver(new SchurLemmaProblem(24));
        s.solve(1);
        assertTrue(s.getSolutions().size() == 0);
    }

    //------------------------------------------------------------------------
    // STATIC PROPERTIES
    //------------------------------------------------------------------------
    private static Category cat = Category.getInstance(SchurLemmaTest.class);
}
